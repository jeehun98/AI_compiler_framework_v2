[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aicf-skeleton"
version = "0.1.0"
description = "Minimal AI framework/compiler skeleton with built-in observability hooks"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
