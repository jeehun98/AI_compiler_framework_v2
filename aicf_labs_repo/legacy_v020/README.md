# Selected v0.20 legacy source snapshots

These files are copied from the previous codegen-centered project artifacts and are **not imported by the new core package**.

Purpose:

1. preserve the proven end-to-end baseline,
2. make migration decisions explicit,
3. move capabilities one boundary at a time into `backend_cuda/` or `codegen_prototype/`.

The snapshots are intentionally incomplete because the prior project was developed as version deltas. Treat them as migration references, not as a second runnable package.
